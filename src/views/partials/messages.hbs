{{#if success_msg}}
    <div class="message" message="{{success_msg}}">
    </div>
    <script>
        const messageContainer = document.querySelector('.message')
        const message = messageContainer.getAttribute('message')     
            Swal.fire({
                html: message,
                showConfirmButton: false,
                timer: 1700,
            });

//        window.addEventListener('load', () => {
//        })
        
    </script>
{{/if}}
{{#if error_msg}}
    <div class="message" message="{{error_msg}}">
    </div>
    <script>
        const messageContainer = document.querySelector('.message')
        const message = messageContainer.getAttribute('message')     

//        window.addEventListener('load', () => {
            Swal.fire({
                html: message,
                showConfirmButton: false,
                timer: 1700,
            });
//        })
        
    </script>
{{/if}}
<link rel="stylesheet" type="text/css" href="../messages.css">